@model IEnumerable<Tor.Models.Category>
 <div class="container p-3">

        <div class="row pt-4">


            <div class="col-6">
                <h2 class="text-primary">Список категорий</h2>
            </div>


            <div class="col-6 text-right">
                <a asp-controller="Category" asp-action="Create" class="btn btn-primary">
                    <i class="fa-solid fa-plus"></i> &nbsp; Добавить новую категорию
                </a>
            </div>


        </div>

<br/><br/>


    @if (Model.Count() > 0)
    {
        <table class ="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Название
                    </th>
                    <th>
                        Перевод
                    </th>
                    <th>

                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach(var cat in Model)
                {
                    <tr>
                        <td width="50%">@cat.Name</td>
                        <td width="30%">@cat.Translate</td>
                        <td class="text-center">
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="Category" asp-route-Id="@cat.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                   <i class="fa-solid fa-pen-to-square"></i> Изменить
                                </a>
                                <a asp-controller="Category" asp-route-Id="@cat.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                    <i class="fa-solid fa-trash-can"></i> Удалить
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else{
        <p> No category exists.</p>
    }

 </div>

