@model IEnumerable<Tor.Models.ApplicationType>
 <div class="container p-3">

        <div class="row pt-4">


            <div class="col-6">
                <h2 class="text-primary">Список коллекций</h2>
            </div>


            <div class="col-6 text-right">
                <a asp-controller="ApplicationType" asp-action="CreateApplication" class="btn btn-primary">
                    <i class="fa-solid fa-plus"></i> &nbsp; Добавить новую коллекцию
                </a>
            </div>


        </div>

<br/><br/>


    @if (Model.Count() > 0)
    {
        <table class ="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Название
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach(var cat in Model)
                {
                    <tr>
                        <td width="50%">@cat.Name</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else{
        <p> No application type exists.</p>
    }

 </div>

